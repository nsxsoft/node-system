import { ModulVue } from '../../../utils/vue/vue';
export declare enum MSteppersItemState {
    Visited = "visited",
    InProgress = "in-progress",
    Disabled = "disabled"
}
export declare abstract class BaseSteppers extends ModulVue {
    abstract setLineWidth(): void;
    abstract setAnim(value: any): void;
}
export declare class MSteppersItem extends ModulVue {
    state: MSteppersItemState;
    iconName: string;
    iconTitle: string;
    completed: boolean;
    url: string;
    private stateChanged;
    private readonly isVisited;
    private readonly isInProgress;
    private readonly isDisabled;
    private readonly isTabIndex;
    private readonly isLink;
    private readonly componentToShow;
    private readonly role;
    private readonly propUrl;
    private onClick;
}
//# sourceMappingURL=steppers-item.d.ts.map