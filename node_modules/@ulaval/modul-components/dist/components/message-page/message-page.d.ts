import { PluginObject } from 'vue';
import { ModulVue } from '../../utils/vue/vue';
/**
 * Utility class to manage the properties related to the link displayed in the error pages.
 */
export declare class Link {
    label: string;
    url: string;
    external: boolean;
    /**
     * Constructor
     * @param label Label for the url link to display.
     * @param url Target for the location to navigate to, can be relative.
     * @param external Defines to the target is external (opens in new tab), or internal (opens in same tab) to the application.
     */
    constructor(label: string, url: string, external?: boolean);
}
export declare enum MMessagePageSkin {
    Default = "default",
    Light = "light"
}
export declare enum MMessagePageImageSize {
    Default = "130px",
    Small = "76px"
}
export declare class MMessagePage extends ModulVue {
    state: string;
    skin: string;
    iconName: string;
    svgName: string;
    imageSize: string;
    title: string;
    hints: string[];
    links: Link[];
    svg: string;
    readonly isSvg: boolean;
    readonly hasHints: boolean;
    readonly hasLinks: boolean;
    readonly styleObject: {
        [name: string]: string;
    };
    readonly propImageSize: string;
    isTargetExternal(isExternal: boolean): string;
    protected created(): void;
    private readonly hasLinksAndSlot;
    private readonly hasBody;
    private readonly isSkinDefault;
    private readonly isSkinLight;
    private readonly isStateInformation;
    private readonly isStateWarning;
    private readonly isStateError;
    private readonly isStateConfirmation;
    private readonly iconNameProp;
}
declare const MessagePagePlugin: PluginObject<any>;
export default MessagePagePlugin;
//# sourceMappingURL=message-page.d.ts.map