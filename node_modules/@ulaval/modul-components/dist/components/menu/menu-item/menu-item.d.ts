import { Location } from 'vue-router';
import { ModulVue } from '../../../utils/vue/vue';
import { Menu } from '../menu';
export declare abstract class BaseMenuItem extends ModulVue {
}
export interface MenuItem {
    group: boolean;
    propOpen: boolean;
    selected: boolean;
    groupSelected: boolean;
    insideGroup: boolean;
}
export declare class MMenuItem extends BaseMenuItem implements MenuItem {
    open: boolean;
    value: string;
    label: string;
    url: string | Location;
    iconName: string;
    disabled: boolean;
    group: boolean;
    selected: boolean;
    groupSelected: boolean;
    insideGroup: boolean;
    menuRoot: Menu | null;
    groupItemRoot: MenuItem | null;
    private internalOpen;
    private ariaControls;
    protected mounted(): void;
    private openChanged;
    propOpen: boolean;
    readonly isAnimReady: boolean;
    private readonly isUrl;
    private toggleOpen;
    readonly isDisabled: boolean;
    private onClick;
}
//# sourceMappingURL=menu-item.d.ts.map