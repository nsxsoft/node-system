import { PluginObject } from 'vue';
import { ModulVue } from '../../utils/vue/vue';
import { MPopperPlacement } from '../popper/popper';
export declare abstract class BaseOption extends ModulVue {
}
export interface MOptionInterface {
    hasIcon: boolean;
    checkIcon(el: boolean): void;
    close(): void;
}
export declare enum MOptionsSkin {
    Light = "light",
    Dark = "dark"
}
export declare class MOption extends BaseOption implements MOptionInterface {
    placement: MPopperPlacement;
    skin: MOptionsSkin;
    openTitle: string;
    closeTitle: string;
    disabled: boolean;
    size: string;
    focusManagement: boolean;
    hasIcon: boolean;
    private open;
    private id;
    checkIcon(icon: boolean): void;
    close(): void;
    private onOpen;
    private onClose;
    private onClick;
    private getOpenTitle;
    private getCloseTitle;
    private readonly propTitle;
    private readonly ariaControls;
}
declare const OptionPlugin: PluginObject<any>;
export default OptionPlugin;
//# sourceMappingURL=option.d.ts.map