import Vue, { PluginObject } from 'vue';
export declare enum MButtonType {
    Button = "button",
    Submit = "submit",
    Reset = "reset"
}
export declare enum MButtonSkin {
    Primary = "primary",
    Secondary = "secondary"
}
export declare enum MButtonIconPosition {
    Left = "left",
    Right = "right"
}
export declare class MButton extends Vue {
    type: MButtonType;
    skin: MButtonSkin;
    precision: string;
    disabled: boolean;
    waiting: boolean;
    fullSize: boolean;
    iconName: string;
    iconPosition: MButtonIconPosition;
    iconSize: string;
    onClick(event: Event): void;
    onFocus(event: Event): void;
    onBlur(event: Event): void;
    private readonly isSkinPrimary;
    private readonly isSkinSecondary;
    private readonly isWaiting;
    private readonly hasIcone;
    private readonly hasIconLeft;
    private readonly hasIconRight;
    private readonly hasWaitingIconLeft;
    private readonly hasWaitingIconRight;
    private readonly hasPrecision;
}
declare const ButtonPlugin: PluginObject<any>;
export default ButtonPlugin;
//# sourceMappingURL=button.d.ts.map