import { PluginObject } from 'vue';
import { ModulVue } from '../../utils/vue/vue';
import { MPopperPlacement } from '../popper/popper';
export declare enum MTooltipMode {
    Icon = "icon",
    Link = "link"
}
export declare enum MTooltipSize {
    Small = "small",
    Large = "large"
}
export declare class MTooltip extends ModulVue {
    open: boolean;
    mode: MTooltipMode;
    placement: MPopperPlacement;
    closeButton: boolean;
    disabled: boolean;
    openTitle: string;
    closeTitle: string;
    className: string;
    size: MTooltipSize;
    private propOpen;
    private id;
    protected mounted(): void;
    private openChanged;
    private readonly propMode;
    private readonly propCloseButton;
    private readonly hasDefaultSlot;
    private onOpen;
    private onClose;
    private onClick;
    private getOpenTitle;
    private getCloseTitle;
    private readonly propTitle;
    private close;
    private readonly ariaControls;
}
declare const TooltipPlugin: PluginObject<any>;
export default TooltipPlugin;
//# sourceMappingURL=tooltip.d.ts.map