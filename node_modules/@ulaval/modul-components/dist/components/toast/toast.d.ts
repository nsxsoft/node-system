import { PluginObject } from 'vue';
import { BackdropMode, PortalMixinImpl } from '../../mixins/portal/portal';
import { ModulVue } from '../../utils/vue/vue';
export declare enum MToastTimeout {
    none = "none",
    short = "short",
    long = "long"
}
export declare enum MToastPosition {
    TopLeft = "top-left",
    TopCenter = "top-center",
    TopRight = "top-right",
    BottomLeft = "bottom-left",
    BottomCenter = "bottom-center",
    BottomRight = "bottom-right"
}
export declare enum MToastState {
    Confirmation = "confirmation",
    Information = "information",
    Warning = "warning",
    Error = "error"
}
export declare class MToast extends ModulVue implements PortalMixinImpl {
    state: MToastState;
    position: MToastPosition;
    timeout: MToastTimeout;
    open: boolean;
    actionLabel: string;
    icon: boolean;
    offset: string;
    $refs: {
        toast: HTMLElement;
    };
    private buttonMode;
    private internalTimeout;
    doCustomPropOpen(value: boolean, el: HTMLElement): boolean;
    getPortalElement(): HTMLElement;
    getBackdropMode(): BackdropMode;
    handlesFocus(): boolean;
    protected mounted(): void;
    private convertTimeout;
    private onAction;
    private onClose;
    private readonly isStateInformation;
    private readonly isStateWarning;
    private readonly isStateError;
    private readonly isStateConfirmation;
    readonly isTop: boolean;
    private readonly isLeft;
    private readonly isCenter;
    private readonly isRight;
    private getIcon;
}
declare const ToastPlugin: PluginObject<any>;
export default ToastPlugin;
//# sourceMappingURL=toast.d.ts.map