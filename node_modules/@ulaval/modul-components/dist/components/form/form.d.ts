import { Form } from '../../utils/form/form';
import { MFormEvents, MFormListener } from '../../utils/form/form-service/form-service';
import { ModulVue } from '../../utils/vue/vue';
import { MMessageState } from '../message/message';
export declare class MForm extends ModulVue {
    form: Form;
    requiredMarker: boolean;
    messageStateError: MMessageState;
    errors: string[];
    listeners: MFormListener[];
    onSubmit(): void;
    onReset(): void;
    readonly hasErrors: boolean;
    created(): void;
    submit(): void;
    reset(): void;
    setListeners(listeners: MFormListener[]): void;
    private handleErrors;
    emit(eventType: MFormEvents, params?: any): void;
}
//# sourceMappingURL=form.d.ts.map