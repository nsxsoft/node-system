import { PluginObject } from 'vue';
import { BackdropMode, PortalMixinImpl } from '../../mixins/portal/portal';
import { ModulVue } from '../../utils/vue/vue';
export declare enum MModalSize {
    FullScreen = "full-screen",
    Large = "large",
    Regular = "regular",
    Small = "small"
}
export declare class MModal extends ModulVue implements PortalMixinImpl {
    size: MModalSize;
    closeOnBackdrop: boolean;
    focusManagement: boolean;
    title: string;
    bodyMaxWidth: boolean;
    padding: boolean;
    paddingHeader: boolean;
    paddingBody: boolean;
    paddingFooter: boolean;
    hasKeyboard: boolean;
    $refs: {
        body: HTMLElement;
        modalWrap: HTMLElement;
        article: HTMLElement;
    };
    private closeTitle;
    closeModal(): void;
    handlesFocus(): boolean;
    doCustomPropOpen(value: boolean): boolean;
    getBackdropMode(): BackdropMode;
    readonly sizeFullSceen: boolean;
    readonly sizeLarge: boolean;
    readonly sizeSmall: boolean;
    getPortalElement(): HTMLElement;
    protected mounted(): void;
    private readonly hasDefaultSlot;
    private readonly hasHeader;
    private readonly hasTitle;
    private readonly hasFooterSlot;
    private backdropClick;
    private readonly isAndroid;
    private onFocusIn;
    private onFocusOut;
}
declare const ModalPlugin: PluginObject<any>;
export default ModalPlugin;
//# sourceMappingURL=modal.d.ts.map