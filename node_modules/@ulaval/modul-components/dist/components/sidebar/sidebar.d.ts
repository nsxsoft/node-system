import { PluginObject } from 'vue';
import { BackdropMode, PortalMixinImpl } from '../../mixins/portal/portal';
import { ModulVue } from '../../utils/vue/vue';
export declare enum MSidebarOrigin {
    Top = "top",
    Right = "right",
    Bottom = "bottom",
    Left = "left"
}
export declare enum MSidebarCloseButtonPosition {
    Left = "left",
    Right = "right"
}
export declare class MSidebar extends ModulVue implements PortalMixinImpl {
    origin: MSidebarOrigin;
    width: string;
    title: string;
    closeButton: boolean;
    closeButtonPosition: MSidebarCloseButtonPosition;
    focusManagement: boolean;
    closeOnBackdrop: boolean;
    padding: boolean;
    paddingHeader: boolean;
    paddingBody: boolean;
    paddingFooter: boolean;
    $refs: {
        baseWindow: HTMLElement;
        article: HTMLElement;
        modalWrap: HTMLElement;
        body: HTMLElement;
    };
    readonly popupBody: HTMLElement;
    handlesFocus(): boolean;
    doCustomPropOpen(value: boolean): boolean;
    getBackdropMode(): BackdropMode;
    getPortalElement(): HTMLElement;
    protected mounted(): void;
    private readonly hasHeaderSlot;
    private readonly hasDefaultSlot;
    private readonly hasHeader;
    private readonly hasTitle;
    private readonly hasFooterSlot;
    private backdropClick;
    private closeModal;
    private readonly marginLeft;
    private readonly propWidth;
}
declare const SidebarPlugin: PluginObject<any>;
export default SidebarPlugin;
//# sourceMappingURL=sidebar.d.ts.map