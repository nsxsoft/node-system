import { PluginObject } from 'vue';
import { Location } from 'vue-router';
import { ModulVue } from '../../utils/vue/vue';
export declare enum MLinkMode {
    RouterLink = "router-link",
    Link = "link",
    Button = "button"
}
export declare enum MLinkIconPosition {
    Left = "left",
    Right = "right"
}
export declare enum MLinkSkin {
    Default = "default",
    Light = "light",
    Text = "text"
}
export declare class MLink extends ModulVue {
    url: string | Location;
    mode: MLinkMode;
    disabled: boolean;
    unvisited: boolean;
    underline: boolean;
    skin: MLinkSkin;
    target: string;
    icon: boolean;
    iconName: string;
    iconPosition: MLinkIconPosition;
    iconSize: string;
    tabindex: string;
    protected mounted(): void;
    private isButtonChanged;
    private onClick;
    private readonly isRouterLink;
    private onKeyup;
    private readonly isButton;
    private readonly isSkinText;
    private readonly isSkinLight;
    private readonly isUnvisited;
    private readonly isIconPositionLeft;
    private readonly isIconPositionRight;
    private readonly hasIcon;
    private readonly propIconName;
    private readonly propUrl;
    private readonly isTargetBlank;
    private readonly routerLinkUrl;
    private readonly routerEvent;
    private isObject;
}
declare const LinkPlugin: PluginObject<any>;
export default LinkPlugin;
//# sourceMappingURL=link.d.ts.map