import { PluginObject } from 'vue';
import { ModulVue } from '../../utils/vue/vue';
export declare enum MStepState {
    Locked = "locked",
    InProgress = "in-progress",
    Success = "success",
    Warning = "warning",
    Error = "error"
}
export declare enum MStepMode {
    Default = "default",
    Accordion = "accordion"
}
export declare class MStep extends ModulVue {
    state: MStepState;
    mode: MStepMode;
    open: boolean;
    required: boolean;
    iconName: string;
    last: boolean;
    private internalOpen;
    private internalMode;
    protected openChanged(open: boolean): void;
    protected mounted(): void;
    protected beforeDestroy(): void;
    private openStep;
    private getIcon;
    private propOpen;
    private readonly propMode;
}
declare const StepPlugin: PluginObject<any>;
export default StepPlugin;
//# sourceMappingURL=step.d.ts.map