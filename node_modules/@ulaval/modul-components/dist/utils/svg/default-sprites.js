var DefaultSpritesPlugin = {
    install: function (v, options) {
        v.prototype.$log.debug('sprites-default.svg', 'plugin.install');
        var svg = (v.prototype).$svg;
        if (svg) {
            svg.addSprites(require('../../assets/icons/sprites-default.svg'));
        }
        else {
            v.prototype.$log.error('DefaultSpritesPlugin.install -> You must use the svg plugin.');
        }
    }
};
export default DefaultSpritesPlugin;
//# sourceMappingURL=default-sprites.js.map