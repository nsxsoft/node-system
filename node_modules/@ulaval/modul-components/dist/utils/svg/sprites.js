import uuid from '../uuid/uuid';
var SpritesService = /** @class */ (function () {
    function SpritesService() {
    }
    SpritesService.prototype.addSprites = function (sprites) {
        var div = document.createElement('div');
        var id = uuid.generate();
        div.id = id;
        div.setAttribute('aria-hidden', 'true');
        div.style.display = 'none';
        div.innerHTML = sprites;
        document.body.insertBefore(div, document.body.childNodes[0]);
        return id;
    };
    return SpritesService;
}());
export { SpritesService };
var SpritesPlugin = {
    install: function (v, options) {
        v.prototype.$log.debug('$svg', 'plugin.install');
        var svg = new SpritesService();
        (v.prototype).$svg = svg;
    }
};
export default SpritesPlugin;
//# sourceMappingURL=sprites.js.map