export var targetIsInput = function (element, event) {
    var recursiveElement = event.target;
    while (recursiveElement && recursiveElement !== element) {
        if (['INPUT', 'TEXTAREA'].find(function (elem) { return elem === recursiveElement.tagName; }) || recursiveElement.contentEditable === 'true') {
            return true;
        }
        recursiveElement = recursiveElement.parentElement;
    }
    return false;
};
//# sourceMappingURL=event.js.map