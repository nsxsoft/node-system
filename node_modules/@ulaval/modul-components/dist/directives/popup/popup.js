import { POPUP_NAME } from '../directive-names';
var MPopupDirective = {
    bind: function (element, binding, node) {
        if (node.context) {
            node.context.$nextTick(function () {
                if (node.context) {
                    node.context.$refs[binding.arg].triggerHook = element;
                }
            });
        }
    }
};
var PopupDirectivePlugin = {
    install: function (v, options) {
        v.directive(POPUP_NAME, MPopupDirective);
    }
};
export default PopupDirectivePlugin;
//# sourceMappingURL=popup.js.map